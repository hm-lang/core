===
An abstract class which has a `swap(NEW_T)` method.  Used instead of a setter
when the previously held value can be returned in addition to being replaced.
===
class NEW_T SWAPPABLE
    ===
    Returns the previous internal value held by this class instance,
    and replaces the internal value with the passed in value.
    ===
    NEW_T MD swap(NEW_T new);


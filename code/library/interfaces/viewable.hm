===
An abstract class which has a `view()` method.  Used in getter-like classes
that can also return an immutable picture of what the internal value is.
===
class NewT Viewable
    ===
    Returns an immutable picture of the internal value held by this class instance.
    Note that the underlying internal value can still change by some other methods,
    but just not by using this method.
    ===
    NewT View ConstThis.view();


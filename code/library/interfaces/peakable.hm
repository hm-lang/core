===
An abstract class which has a `peak()` method.  Used in a `iterator.peakable`
to indicate that the next value that will appear in the iteration can be seen
without changing the `iterator` or any underlying `container` in any way.
===
class NewT peakable()
    ===
    Returns a view (reference to constant value) of the next element,
    or null if there are no further elements to iterate over.
    ===
    NewT View? peak();
